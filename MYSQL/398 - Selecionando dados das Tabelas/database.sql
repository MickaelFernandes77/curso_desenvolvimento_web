-- Selecionar todas as colunas por tabela.

SELECT * FROM tb_cursos;

-- Podemos passar a quantidade e quais colunas desejamos exibir também
SELECT id_curso, nome_curso, imagem_curso, ativo, investimento FROM tb_cursos;